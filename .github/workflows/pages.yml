name: Pages

on:
  push:
    branches:
      - main
    paths:
      - docs/**
      - include/web/mcp2515_dashboard_ui.src.h
      - scripts/build_pages_onboarding.py
      - README.md
      - CHANGELOG.md
      - VERSION
      - THIRD_PARTY_LICENSES
      - .github/workflows/pages.yml
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  pages: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.x"
      - name: Build interactive onboarding preview
        run: python scripts/build_pages_onboarding.py
      - uses: actions/configure-pages@v5
      - uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site
      - uses: actions/upload-pages-artifact@v5
        with:
          path: ./_site
      - id: deployment
        uses: actions/deploy-pages@v5
